#include <vector>

using namespace std;

#ifndef CANPARTITION_H

#define CANPARTITION_H

bool canPartition(vector<int>& nums);

//bool dfs(vector<int>& nums, int startIdx, int tempSum, int target);

#endif // !CANPARTITION_H
